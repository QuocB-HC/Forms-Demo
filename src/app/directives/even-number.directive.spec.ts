import { EvenNumberDirective } from './even-number.directive';

describe('EvenNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenNumberDirective();
    expect(directive).toBeTruthy();
  });
});
